# 实验结果存放目录

运行 `run_experiment.py` 或 `run_mininet_topo.py` 后，生成的CSV或JSON结果文件将保存在此目录。

文件命名格式：
- `experiment_YYYYMMDD_HHMMSS.csv`  (模拟实验结果)
- `mininet_result.json`              (Mininet实验结果)

这些文件可直接用于论文图表绘制。
